function [X] = AXXB_Solver(A, B)
    % AX = XB
    % A = [R_A, t_A; zeros(1,3), 1]
    % B = [R_B, t_B; zeros(1,3), 1]
    % X = [R_X, t_X; zeros(1,3), 1]
    % R_A * R_X = R_X * R_B
    % R_A * t_X + t_A = R_X * t_B + t_X
end

